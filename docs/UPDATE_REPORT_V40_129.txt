V40_129 — Опция тумбы 1-в-1 как Альте + Арбе
================================================
Дата: 2026-05-24

ЗАДАЧИ
-------
1. Тумба Арбе — забыл (не отображалась опция в обычной карточке).
2. Сделать всё «1-в-1» как у шкафа Альте: блок «Итог комплектации» в
   правой колонке (фото 4) и попап «Опции» (фото 2-3). Фото в карточках
   попапа не нужно (только для шкафа были).

ЧТО ИСПРАВЛЕНО

1) АРБЕ
   Проверено: тумба Арбе (8027372052) есть и в catalog.js и в seat-options.js.
   Опция не отображалась потому, что у Арбе нет мультиобъявления →
   #mMultiSizes скрыт → mountTumbaSeatToolsHost не находил inline-host
   и кнопка падала в fallback на .mGal, оставаясь почти невидимой
   слева под фото. После переделки в стиль Альте кнопка корректно
   позиционируется в обоих случаях.

2) БЛОК «ИТОГ КОМПЛЕКТАЦИИ» — структура 1-в-1 как у Альте (фото 4):
   • «Итог: <название тумбы>»
   • «Комплектация: тумба + мягкое сиденье» (или «только тумба»)
   • «В комплекте:»
     - «<тумба> — 1 шт.»
     - «<сиденье> — 1 шт.»  (если выбрано)
     - «Цвет сиденья: <цвет>»
   • «Наличие комплекта: ✓ В наличии — N шт.» (или «Под заказ»)
   • Foot-бейдж со статусом
   
   Цена сверху модалки (#mPri) обновляется автоматически:
   тумба + сиденье если выбрано.

3) ПОПАП — стиль .wardrobeOpt-pop (Альте), но БЕЗ .waOpt-choice-visual
   (фото не нужно для опции сиденья):
   • Заголовок «Опции тумбы» + подзаголовок
   • Кнопка ×
   • 2 карточки `.waOpt-choice` с радио сверху-слева:
     - «Только тумба» +0 ₽ + бейдж наличия
     - «Тумба + мягкое сиденье» +X ₽ + название сиденья · цвет + бейдж
   • Подсветка активной карточки (зелёный border + glow)
   • «Итог: <X ₽>» + «<наличие>» + кнопка «Готово»
   • Бейджи в карточках и в футере обновляются при выборе

4) ВЫБОР НЕ ЗАКРЫВАЕТ ПОПАП
   setSeatOptionChoice → updateSeatOptionPopup (а не close) — попап
   остаётся открытым, обновляются все бейджи в реальном времени.

5) BLOCK + КНОПКА:
   • Блок «Итог комплектации» — в правой колонке модалки (`.mInf`)
   • Кнопка «⚙️ Выбрать опции» — в `.mMulti-controls` рядом с селектом
     размера если он есть, иначе fallback в `.mGal`

6) NAЛИЧИЕ КОМПЛЕКТА — реальное по STOCK:
   • Без сиденья: статус тумбы (✓ N шт. / Под заказ)
   • С сиденьем: И тумба И сиденье должны быть на складе
     - Оба есть → «✓ Комплект с сиденьем в наличии — N шт.»
     - Только тумбы нет → «Тумба под заказ»
     - Только сиденья нет → «Сиденье под заказ»
     - Обоих нет → «Комплект под заказ»

ФАЙЛЫ ИЗМЕНЕНЫ
---------------
• index.html: renderTumbaSeatOption + updateTumbaSeatOptionSelection
  (структура итога Альте), openMiniSeatOptionPopup на .wardrobeOpt-pop,
  updateSeatOptionPopup на .waOpt-choice, CSS под seat-попап
• Cache-buster: V40_128 → V40_129

ПРОВЕРКА
---------
✅ validate-catalog.py: 1918 / 1918 / 0 расхождений
✅ node -c всех JS: clean
✅ Все 6 тумб с опцией (Арбе, Кевин 600 ×2, Кевин 800 ×2, Мэнкс 800)
   корректно загружают цены из catalog.js + цены сиденья из seat-options.js
